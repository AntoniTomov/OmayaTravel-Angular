{
  "name": "omaya-travel-angular",
  "version": "0.0.0",
  "private": true,
  "description": "Omaya Travel Angular rebuild monorepo.",
  "packageManager": "pnpm@10.11.1",
  "scripts": {
    "build": "pnpm --dir apps/website run build",
    "lint": "pnpm --filter website run format:check",
    "type-check": "pnpm --dir apps/website run build --configuration development",
    "test": "pnpm --dir apps/website run test"
  },
  "engines": {
    "node": ">=22.22.3",
    "pnpm": ">=10.0.0"
  }
}
